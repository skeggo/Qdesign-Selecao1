# CORS — comma-separated list of allowed frontend origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3002

# Qdrant vector database
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Gemini / LLM API key (if used)
GEMINI_API_KEY=
